<h1>My articles</h1>

<ul>
  <% @articles.each do |article| %>
    <li style="list-style-type: none">
      <%= link_to article.title, article_path(article) %> <%= link_to '🗑️', article_path(article), data: {turbo_method: :delete, turbo_confirm: "Are you sure?"} %>
    </li>
  <% end %>
</ul>

<%= link_to 'Add a new article', new_article_path %>
